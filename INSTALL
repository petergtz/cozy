Installation Instructions
==========================

Open a terminal and cd to current directory. Then type:
	make install
or
	make PREFIX=/my/prefix install

Cozy is written in Python and therefore need not be built.

For run-time dependencies of Cozy, please have a look at the DEPENDENCIES file.

IMPORTANT: you need a version of Nautilus with DBus-support for Cozy. 

